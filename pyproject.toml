[project]
name = "my-suse-machine"
version = "0.1.0"
description = "My dev machine will survive!!"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ansible-core>=2.20.3",
    "ansible-lint",
    "yamllint",
]
